package crm.service;

import crm.domain.Systemdictionary;
import crm.page.PageResult;
import crm.query.QueryObject;

import java.util.List;

public interface ISystemdictionaryService {
    int save(Systemdictionary obj);
    int update(Systemdictionary obj);
    int delete(Long id);
    Systemdictionary get(Long id);
    List<Systemdictionary> selectAll();
    PageResult queryByCondition(QueryObject qo);
}
